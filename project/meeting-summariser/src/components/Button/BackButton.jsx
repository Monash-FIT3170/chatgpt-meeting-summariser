import './Button.css'

const BackButton = () => {
    const handleChange = (e) => {
		// on click

        // back home page 
        
	}
		return (
		<div className= "back_button" onClick={(e) => handleChange(e)}>
			Back
		</div>
	);
};

export default BackButton;
